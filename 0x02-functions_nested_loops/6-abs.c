#include "main.h"

/**
 *abs - compute absolute value
 *@n: integer value
 *Return: absolute value
 */

int _abs(int)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	return (0);
}
